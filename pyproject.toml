[project]
name = "flo101-proofmark"
version = "0.1.0"
description = "Track B Content Curator + Dynamic Case Builder for the flo101 Applied AI assessment."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
proofmark = "proofmark.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
